@model IEnumerable<WasJohnLate.Models.EntryTime>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RecorderName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EntryDateTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WasJohnLate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EntryLocation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RecorderName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EntryDateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WasJohnLate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EntryLocation)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
